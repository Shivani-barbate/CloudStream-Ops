apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api-ingress
  namespace: cloudstream-uat

spec:
  ingressClassName: nginx

  rules:
  - host: uat-api.cloudstream.local

    http:
      paths:
      - path: /
        pathType: Prefix

        backend:
          service:
            name: api-service
            port:
              number: 80