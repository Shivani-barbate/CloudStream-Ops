apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: grafana-letsencrypt
  namespace: monitoring
spec:
  secretName: grafana-letsencrypt-secret

  dnsNames:
    - grafana.shivanicloud.online

  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer